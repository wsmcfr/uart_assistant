# 更新日志

本文档记录 ComAssistant 的版本更新历史。

## [1.8.0] - 2026-06-02

### 新增功能
- 新增串口接收区统一右键菜单，文本、十六进制和过滤结果视图均支持复制、全选、清屏、暂停/继续显示、自动滚动、HEX显示、时间戳和高亮切换。
- 新增接收区显示暂停状态，暂停期间仍保留接收缓存和协议链路数据，继续显示后会补刷暂停期间收到的内容。

### 优化改进
- 全面刷新浅色与暗色主题覆盖层，统一主窗口、工具窗口、弹窗、右键菜单、消息框、表格、按钮和输入控件的桌面工具视觉层级。
- 暗色主题恢复为用户原有 Apple 深色背景体系，同时保留默认操作、危险操作、禁用状态和焦点态的清晰视觉区分。
- 设置、导出、文件传输、恢复、工具箱和数据分窗配置等弹窗优化页脚、按钮语义、布局间距和危险操作样式。
- 设置窗口、工具箱编码页和常见表单控件增加稳定尺寸约束，避免带单位数值框、下拉框和长文本被裁剪或遮挡。
- 英文翻译与内置帮助同步新增接收区右键菜单、主题覆盖范围和控件防裁剪说明。

### Bug 修复
- 修复接收区右键菜单只显示 Qt 默认 `Copy / Select All`，无法直接清屏或暂停/继续显示的问题。
- 修复暂停显示期间切换 HEX 显示会提前刷出暂停期间接收数据的问题。
- 修复设置窗口显示页中 `最大行数`、`HEX缓冲区`、`换行符` 等字段文字和单位可能被裁剪的问题。
- 修复工具箱编码转换页在暗色主题和紧凑窗口下，字符编码转换输入框、下拉框和转换按钮互相遮挡的问题。
- 修复暗色主题覆盖层偏离原有深色背景、部分弹窗和菜单视觉不一致的问题。

### 升级说明
- 本版本不改变通信协议或配置文件结构，可直接覆盖升级。
- 接收区“暂停显示”只暂停界面刷新，不会暂停串口、网络或 HID 数据接收；需要停止真实通信时仍应断开当前连接。

---

## [1.7.0] - 2026-06-02

### 新增功能
- 新增协议能力平台注册中心，内置协议统一暴露稳定协议 ID、分类、绘图能力、构帧能力、配置 Schema 与诊断数据。
- 新增 Lua 脚本接收协议 `lua.script`，可通过“工具 -> 接收协议 -> Lua Script”进入主窗口接收解析链路，并调用 `process(data, context)` 解析接收数据。
- 新增协议配置与协议诊断入口，支持当前协议配置保存、规范化、诊断 JSON 复制/保存和 Lua 最近错误查看。

### 优化改进
- 协议配置 UI 支持 `scriptSource` 多行 Lua 脚本编辑、字段级错误提示和空 Schema 默认行为文案。
- 会话保存/恢复改为以稳定协议 ID 为事实源，加载旧会话时仍可按旧版绘图协议类型迁移到兼容配置。
- Lua 协议补强脚本错误展示、最近错误记录、成功解析清空错误、输出边界诊断和沙箱边界导出。
- 英文翻译与内置帮助同步新增接收协议、协议配置、协议诊断和 Lua 脚本接收流程说明。

### Bug 修复
- 修复 Lua 协议已可创建但无法从主窗口用户可选接收链路进入的问题。
- 修复非旧版协议无法按稳定 `protocolId` 保存和恢复，加载后可能回落到旧枚举协议的问题。
- 修复协议配置校验错误只显示总错误、用户难以定位具体字段的问题。

### 升级说明
- `lua.script` 已进入接收解析链路，但仍不开放 `serial.receive(timeout)`、外部 `scriptPath` 加载和脚本化构帧；旧版“绘图协议”菜单继续只列 Raw/TEXT/STAMP/CSV/JustFloat。
- Lua 脚本配置会随会话保存。升级后建议先在“工具 -> 协议诊断...”确认当前协议 ID、配置版本和最近错误，再进行复杂脚本调试。

---

## [1.6.0] - 2026-06-01

### 新增功能
- 新增工具栏“发送文件”入口，打开统一文件发送中心，可直接从主界面发送 Keil/GD32 工程生成的 `.bin`、`.hex` 或其他文件。
- 新增裸流分块发送模式，支持自定义每包字节数、包间隔、暂停、继续、取消，并在发送前显示文件大小、CRC32、分块数和预计耗时。
- 新增自定义 OTA 发送模式，支持文件头、数据块、结束包、Magic、块大小、包间隔、可选 ACK、ACK 超时和重试配置。

### 优化改进
- 文件传输对话框升级为三模式界面，保留 XMODEM、XMODEM-CRC、XMODEM-1K 和 YMODEM 标准协议发送/接收能力。
- 主窗口会把串口接收数据转发给文件传输状态机，保证 XMODEM/YMODEM 握手和自定义 OTA ACK 模式能正常推进。
- 文件发送相关英文翻译与翻译完整性测试已同步补齐，帮助文档新增三种文件发送模式说明。

### Bug 修复
- 修复从 IAP 升级入口打开后再从普通文件传输入口打开时，窗口可能保留 IAP 模式状态的问题。
- 修复文件传输窗口销毁时对已完成传输再次发送取消语义数据的风险。

### 升级说明
- 自定义 OTA 第一版提供通用包格式，设备端需按文件头、数据块和结束包格式解析；若设备已有专用 Bootloader，请优先选择其匹配的 XMODEM/YMODEM 或裸流模式。
- 裸流和 OTA 发送大文件时建议先使用较小块大小与较大包间隔联调，确认 MCU 缓冲区与 Flash 写入速度后再提升吞吐。

---

## [1.5.0] - 2026-06-01

### 新增功能
- 新增通信类型专用工作台打磨版：TCP客户端、TCP服务器、UDP 与 HID 均提供独立状态摘要、日志工具和发送辅助。
- TCP服务器新增客户端数量提示与"断开选中"入口，可从专用工作台主动断开指定客户端。
- UDP 工作台新增最近远端清空、广播目标摘要、发送字节数提示和快捷发送能力。
- HID Report 工作台新增 Output/Feature 完整 Report 预览、payload 字节数、截断提示、HEX格式化、历史复制和清空。

### 优化改进
- TCP/UDP 发送区新增 `Ctrl+Enter` 快速发送、追加换行、HEX格式化和字节数提示。
- 网络工作台日志统一显示方向、类型、字节数、HEX 与文本摘要，并支持自动滚动、复制和清空。
- HEX 输入规范化改为按输入片段补齐，`0x0a bb c` 会格式化为 `0A BB 0C`，更符合调试输入习惯。
- 帮助文档同步补充通信工作台、HID Report 预览、自动滚动、HEX格式化和快捷发送说明。

### Bug 修复
- 修复 TCP/UDP/HID 已有专用工作台仍偏串口式收发界面、缺少协议对应调试入口的问题。
- 修复测试辅助在 Release 构建下缺失控件时可能空指针崩溃的问题，改为给出明确缺失控件错误。

### 升级说明
- 本版本继续使用 Qt 5.12.9 与 hidapi；建议需要 TCP/UDP/HID 专用调试界面的用户升级。
- HID payload 超过 Report 可用长度时会显示截断提示，请按设备协议确认 Report 长度、Report ID 与是否需要分包。

---

## [1.4.0] - 2026-06-01

### 新增与增强
- 新增 USB HID 通信后端，支持通过 hidapi 枚举、打开、发送和接收 HID 报告。
- 工具栏通信类型新增 `HID`，可复用设备下拉框选择 HID 设备，并在 HID 模式下隐藏串口波特率和网络地址端口控件。
- 会话、配置和通信工厂补齐 HID 配置保存与恢复，包含设备路径、VID/PID、接口号、Usage、输入/输出报告长度和 Report ID 相关配置。
- 补充 HID 通信工厂、会话持久化、发布元数据与翻译完整性回归测试。

### 修复
- 修复 USB HID 长期停留在计划项、代码中无可用实现的问题。
- 修复 README 与内置帮助文档中的最新稳定版发布日期不一致风险。
- 修复英文翻译中未完成条目导致的发布前完整性风险。
- 修复绘图窗口生命周期测试中复用裸指针地址判断对象身份造成的偶发失败风险。

### 升级说明
- 发布构建会通过 vcpkg manifest 安装 `hidapi` 并启用真实 HID 后端；未安装 hidapi 的本地构建仍可编译，但运行时会提示当前构建未启用 HID 后端。
- 本版本新增 HID 通信能力，建议需要 USB HID 调试的用户升级。

---

## [1.3.3] - 2026-05-06

### 新增与增强
- 新增显示设置统一策略，集中管理接收区显示默认值，并在升级时自动迁移旧配置。
- 补充显示策略、绘图渲染策略、绘图窗口生命周期与接收区缓存相关单元测试，降低后续回归风险。

### 性能优化
- 优化高频绘图场景下的内存占用，默认改为按需启用 OpenGL，并调整默认多重采样策略。
- 优化 OpenGL 缓冲区、FBO、绘图缓存与窗口关闭链路的资源释放时机，减少停止绘图、关闭窗口与关闭 OpenGL 后的常驻内存。
- 优化接收区、数据表、数据分窗与叠加层缓冲策略，在保持显示质量与交互体验的前提下降低长期运行占用。

### 修复
- 修复绘图窗口关闭后旧对象未及时销毁，后续继续绘图时残留占用未及时回收的问题。
- 修复 OpenGL 开关显示状态与实际渲染状态不同步的问题。
- 修复工具箱结果区域在深色主题下出现白色背景的问题。
- 修复设置页默认 HEX 缓冲区仍显示旧默认值、实际运行值与界面不一致的问题。
- 修复绘图窗口默认尺寸偏小，首次打开时内容显示不完整的问题。

### 升级说明
- 升级到 `v1.3.3` 后，旧版本默认 `8MB` 的 HEX 缓冲区会自动迁移为 `2MB`。
- 如需更大的接收缓冲区，可在设置页面中手动调整。

---

## [1.3.2] - 2026-05-05

### 新增与增强
- 终端模式补齐 Linux 风格交互细节，支持基于当前输入前缀的 Tab 补全与多候选逐项展示。
- 补充终端模式、ANSI 解析、FFT、滤波与翻译完整性等单元测试，提升后续回归验证覆盖率。

### 性能优化
- 优化高频接收场景下的 UI 刷新链路，减少接收区与终端视图在大吞吐数据下的卡顿。
- 优化实时绘图与频谱分析窗口的重绘节流、点数控制与渲染策略，在保持绘图质量不变的前提下提升流畅性。
- 调整 QCustomPlot OpenGL 使用与回退策略，降低部分显卡/驱动环境下的兼容性风险。

### 修复
- 修复中英文切换后部分工具栏、二级目录页面与模式页面文案未同步刷新的问题。
- 修复终端模式下输入区与显示区分离、回车后本地输入被错误清空、Tab 后光标异常等交互问题。
- 修复终端模式本地发送后错误回显自身内容的问题，改为仅显示设备真实返回数据；无返回时只换到下一行提示。
- 修复串口模式、帧格式模式及其他模式顶部间距、压缩与英文文案占位不均导致的布局显示问题。
- 修复主题切换部分场景不可选、白色区域显示突兀等样式问题。
- 修复自动更新说明缺失时 Release 页面仅显示占位提示的问题，后续版本要求先同步 `CHANGELOG.md` 再发布。

### 升级说明
- 本版本继续使用 Qt 5.12.9，不涉及配置迁移；建议直接覆盖升级并保留原有会话/配置文件。
- 以后每次发布前，必须先补齐对应版本的 `CHANGELOG.md` 段落，否则 GitHub Release 正文会退回占位说明。

---

## [1.1.3] - 2026-03-11

### 新增
- 设置中可配置 HEX 缓冲区容量
- 接收区新增性能统计面板（接收速率/渲染耗时）

### 性能优化
- 接收区高亮改为块级高亮，显著降低卡顿
- HEX 视图改为 Model/View + 环形缓冲，提升高吞吐下的流畅性
- 主文本改为 QPlainTextEdit，滚动更平滑且内存更友好

---

## [1.1.2] - 2026-03-10

### 性能优化
- 接收区文本高亮改为延迟批处理，减少高频数据下全文扫描造成的卡顿
- 十六进制视图改为批量更新与单元复用，降低大量字节追加时的 UI 阻塞
- 终端模式显示刷新加入节流，并对历史缓存做上限控制，提升长时间运行稳定性

### 功能增强
- 新增“设置 -> 差值曲线实时更新”开关，可按需暂停差值实时计算
- 新增“设置 -> 滤波曲线实时更新”开关，可按需暂停滤波实时计算
- 新增工具栏性能诊断信息（FPS/平均耗时/点数），便于定位实时绘图瓶颈

### 修复
- 修复接收区历史缓存与原始缓存长期累积导致的性能退化
- 修复清空接收区后终端缓冲状态未完全复位的问题

---

## [1.1.1] - 2026-03-10

### 新增与恢复
- 新增应用内版本检查能力，支持从 GitHub Releases 检查最新稳定版
- 新增“帮助 -> 检查更新...”入口，支持手动触发更新检查
- 新增“帮助 -> 启动时自动检查更新”开关，默认按天自动检查
- 恢复并完善曲线对比能力，支持双曲线对比与差值曲线展示

### 性能优化
- 优化大数据量场景下的波形刷新策略，降低高频重绘导致的卡顿
- 优化滚动窗口与实时绘图协同逻辑，减少数据显示与渲染竞争
- 优化绘图路径在长时间运行下的响应稳定性

### 修复
- 修复部分绘图功能在升级后缺失的问题（含对比/差值相关能力）
- 修复自定义 X 数据路径与实时更新链路中的异常行为
- 修复发布流水线缺失 Qt 组件导致构建失败的问题

### 发布与版本管理
- 建立 GitHub Releases 标准化发布流程（标签触发自动构建与上传资产）
- 发布资产统一为 `ComAssistant_<tag>_Windows_x64_Portable.zip` 与 `SHA256SUMS.txt`
- 支持用户在 Releases 页面直接下载可运行版本，无需下载源码

---

## [1.2.0] - 2026-01-21

### 新增功能

#### 绘图增强
- **直方图视图**：支持将曲线数据以直方图形式显示，自动计算分布统计
- **XY 绘图模式**：支持选择两个通道作为 X-Y 坐标绘制（李萨如图形等）
- **多窗口同步**：多个绘图窗口可同步暂停/继续状态
- **JustFloat 协议**：支持 VOFA+ 风格的二进制浮点协议，传输效率更高

#### 交互式控件（VOFA+ 风格）
- **控件面板**：可添加多种交互式控件进行实时调参
- **滑动条控件**：拖动即可发送参数，支持自定义发送模板
- **3D姿态显示控件**：实时显示 IMU/飞控的 Roll/Pitch/Yaw 姿态数据
- **控件单独删除**：右键菜单支持删除单个控件
- **控件配置保存**：支持保存/加载控件面板配置

#### 数据管理
- **智能滚屏**：向上滚动时自动暂停刷新，滚动到底部自动恢复
- **数据分窗**：根据关键字/正则表达式将数据路由到不同窗口显示
- **数据表格视图**：以表格形式查看收发数据，支持过滤、搜索、导出

### 改进
- 绘图窗口视图模式切换优化，直方图/XY模式数据持续更新时正常显示
- 绘图协议选择增加 JustFloat 选项

### 技术特性
- 新增 JustFloat 协议解析器（二进制 IEEE754 浮点数组 + 帧尾 00 00 80 7F）
- PlotterManager 增加窗口同步机制

---

## [1.1.0] - 2026-01-16

### 新增功能
- 设置对话框（常规/串口/显示设置）
- 启动恢复对话框（检测未保存会话）
- 调试模式发送功能
- 帮助文档模式说明（Ctrl+C/D/Z 等控制字符说明）
- TCP 客户端/服务器/UDP 通信类型切换

### 改进
- 深色主题颜色适配优化
- 调试模式数据详情视图主题感知

### 修复
- 终端模式状态栏显示问题

---

## [1.0.0] - 2026-01-16 (正式发布)

### 新增功能

#### 通信模块
- 串口通信支持（自动检测、高波特率 115200-921600）
- TCP 客户端/服务器通信（底层支持）
- UDP 通信支持（底层支持）

#### 工作模式
- **串口模式**：标准串口收发，支持文本/HEX显示
- **终端模式**：类终端交互，支持 Ctrl+C/D/Z 控制字符
- **帧模式**：自定义帧格式（帧头/帧尾/校验）
- **调试模式**：数据记录、断点、时间差分析

#### 数据显示
- 多标签数据显示（文本、十六进制、过滤）
- 时间戳显示
- 数据统计（发送/接收字节数）
- 快捷发送功能（10组预设）

#### 曲线绘图
- 实时曲线绘图（QCustomPlot）
- TEXT 协议支持：`{windowId}data1,data2,...`
- STAMP 协议支持：`<timestamp>{windowId}data1,...`
- CSV 协议支持：`windowId,x,y1,y2,...`
- 频谱分析（FFT）
- OpenGL 加速绘图

#### 脚本与工具
- Lua 脚本引擎
- 工具箱（校验和、进制转换、编码转换）
- CRC16/CRC32 计算
- Base64/URL 编码转换
- IEEE754 浮点数转换

#### 数据安全
- 会话保存/恢复（.cas 格式）
- 数据导出（TXT、CSV、HTML、JSON）
- 自动保存功能（30秒间隔）
- 崩溃恢复机制

#### 界面与国际化
- 主题切换（明亮/暗黑）
- 多语言支持（中文/英文）
- 帮助文档系统
- 高 DPI 支持

### 技术特性
- 基于 Qt 6.8.x 开发
- C++17 标准
- 模块化架构设计
- 单元测试覆盖（44个测试用例）
- NSIS 安装包支持

### 已知限制
- 仅支持 Windows 10/11 (x64)
- 最大绘图点数：10000点
- 最大接收缓冲：1MB
- TCP/UDP 功能需通过工具栏切换（v1.1.0 新增）

---

## 版本号说明

版本号格式：`主版本.次版本.修订号`

- **主版本**：重大功能更新或不兼容的 API 变更
- **次版本**：新功能添加，向后兼容
- **修订号**：Bug 修复和小改进

---

## 计划中的功能

### 后续功能
- [ ] 数据记录器（长时间记录）
- [ ] 自定义协议编辑器
- [ ] 批量发送功能
- [ ] 更多校验算法
- [ ] Modbus 主站/从站模式
- [ ] 数据回放功能
- [ ] 插件系统

---

## 反馈与贡献

如果您发现 Bug 或有功能建议，请通过以下方式反馈：

- GitHub Issues: https://github.com/wsmcfr/uart_assistant/issues
- Email: feedback@comassistant.com

感谢您的支持！
